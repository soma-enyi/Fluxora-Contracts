[package]
name = "fluxora_stream"
version = "0.1.0"
edition = "2021"
description = "Fluxora USDC streaming contract for Stellar Soroban"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
